# PalmSDK 0.2 (Redux)

This folder contains the SDK itself: command line tools and sample code.

It requires novacomd and Java. Optional but recommended is novacom.

If you're unsure how to proceed, run install.sh in the parent folder.